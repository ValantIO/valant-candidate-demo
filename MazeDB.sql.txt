CREATE DATABASE MazeDB;
USE MazeDB;

CREATE TABLE Maze (
	MazeId INT NOT NULL IDENTITY,
	Content TEXT NOT NULL,
	Name VARCHAR(100) NOT NULL
)

ALTER TABLE Maze ADD
CONSTRAINT PK_Maze PRIMARY KEY (MazeId);